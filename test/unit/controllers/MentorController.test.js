"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/MentorController');

describe('controllers:MentorController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
