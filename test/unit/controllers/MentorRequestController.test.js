"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/MentorRequestController');

describe('controllers:MentorRequestController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
