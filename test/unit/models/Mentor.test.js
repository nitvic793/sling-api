"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Mentor');
const Fixture = require('../../fixtures/MentorFixture');

describe('models:Mentor', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
