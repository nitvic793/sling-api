"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/MentorRequest');
const Fixture = require('../../fixtures/MentorRequestFixture');

describe('models:MentorRequest', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
